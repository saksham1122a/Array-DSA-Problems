Kadane’s Algorithm (Maximum Subarray Sum)

Problem Statement: -

You are given numbers (positive + negative).
You must find the largest sum you can get by taking numbers continuously. 

Example
[-2, 1, -3, 4, -1, 2, 1]

Best part is:

[4, -1, 2, 1] → sum = 6

            Idea: -

Imagine you are adding numbers one by one.
If your sum becomes negative, it’s useless
A negative sum will only make the next sum smaller
So when sum becomes negative, throw it away and start fresh

That’s Kadane’s Algorithm.

            Step-by-Step Thinking

Start with the first number
Keep adding numbers
If the sum becomes worse than just taking the current number alone → restart
Keep track of the best sum ever

Logic Code: - 

int currentSum = nums[0]; currentSum → sum we are building right now
int maxSum = nums[0];  maxSum → best sum we have ever seen

for(int i = 1; i < nums.size(); i++)   We go through numbers one by one

currentSum = max(nums[i], currentSum + nums[i]);
This means: “Should I continue adding, or start fresh from here?”

Example:
        currentSum = -3
        nums[i] = 5

        max(5, -3 + 5) = 5 is greator
So we start fresh.

maxSum = max(maxSum, currentSum);   Just store the best sum we have found.
return maxSum;

